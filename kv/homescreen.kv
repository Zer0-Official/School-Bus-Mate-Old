<HomeScreen>:

    BoxLayout:
        orientation: 'vertical'
        size: root.width, root.height

        Label:
            text: 'Home Screen'
            font_size: 32
            color: 0,0,0,1

        Button:
            text: 'Go Back'
            font_size: 32
            on_release: root.manager.current = 'login'
